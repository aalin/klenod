[package]
name = "native"
version = "0.0.1"
edition = "2021"

[lib]
name = "native"
crate-type = ["cdylib"]

[dependencies]
magnus = "0.8.2"
swc_core = { version = "73.0.0", features = ["common", "ecma_ast", "ecma_codegen", "ecma_parser", "ecma_parser_typescript", "ecma_transforms_typescript", "ecma_visit"] }
