# frozen_string_literal: true

require "rake/extensiontask"

Rake::ExtensionTask.new("native") do |extension|
  extension.lib_dir = "lib/klenod/plugin/javascript"
  extension.ext_dir = "ext/klenod/plugin/javascript/native"
end

task default: :compile
