#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "klenod"
require_relative "../lib/tools/route_report"
require_relative "../lib/web_config"

config = Example::WebConfig.build_config(mode: :development)
Example::Tools::RouteReport.new(config).print
