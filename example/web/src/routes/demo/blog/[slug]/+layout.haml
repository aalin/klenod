:ruby
  Posts = import_glob("../_posts/*.toml").values.sort_by { it.fetch("date") }.reverse

%article
  %aside
    %h2 Blog posts
    %nav(aria-label="Blog posts")
      = Posts.map do |post|
        %a{ href: "/demo/blog/#{post.fetch("slug")}" }= post.fetch("title")
  %section
    = $children
