:ruby
  PageHeader = import("/components/PageHeader.haml")

%article
  %PageHeader{
    kicker: "Intercepted route",
    title: "Login intercept",
    summary: "This route uses the (...) intercepted segment form."
  }

%title= "Login intercept"
