# Imported Markdown

This content comes from `article.md`, but it renders as a Klenod component.

> This blockquote is mapped through `markdown-components.rb`, so it is not just a raw HTML tag.

```ruby
Article = import("./article.md")
```

The [link component](/demo/markdown) is mapped too.
