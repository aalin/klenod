- tabs = $tabs || []
- name = $name || "tabs"
.tabs
  = tabs.each_with_index.map do |tab, index|
    - slot = tab[:slot] || tab[:id]
    %details{
      name: name,
      open: index.zero?,
    }
      %summary= tab[:title]
      .panel= $children[slot]
