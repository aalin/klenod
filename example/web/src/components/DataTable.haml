:ruby
  def initialize(columns:, rows:, children: nil)
    @columns = columns
    @rows = rows
  end

%table
  %thead
    %tr
      = @columns.map do |column|
        %th= column
  %tbody
    = @rows.map do |row|
      %tr
        = row.map do |cell|
          %td= cell
