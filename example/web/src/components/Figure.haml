%figure
  = if $children&.any?
    = $children
  = else
    %img.image{ src: $image.src, srcset: $srcset || $image&.srcset, sizes: $sizes || $image&.sizes, width: $image.width, height: $image.height, alt: $alt }
  %figcaption= $caption
