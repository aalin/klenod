:ruby
  def initialize(name:, tagline:, image:, params: {})
    @filters = params.fetch(:filters)
  end

%article
  %h1 Shop optional catch-all
  = if @filters.empty?
    %p No filters selected
  = else
    %p= "Filters: #{@filters.join(", ")}"
