:ruby
  def initialize(children:, slots: {})
    @children = children
    @slots = slots
  end

%article
  = if @slots[:sidebar]
    %nav
      = @slots[:sidebar]
  %section
    = @children
  = if @slots[:modal]
    %dialog(open=true)
      = @slots[:modal]
